const animationConfig = {
    start: 0,
    duration: 1000, // Duration in milliseconds
    delay: 200      // Delay before starting the animation
};

// Example usage:
console.log(animationConfig.start);    // Output: 0
console.log(animationConfig.duration); // Output: 1000
console.log(animationConfig.delay);    // Output: 200
